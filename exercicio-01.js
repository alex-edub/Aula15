const A=+prompt('Digite um número:');
const B=+prompt('Digite outro número:');
const C=+prompt('Digite outro número:');

if (A + B < C) {
    alert('O numero A + B é menor');
} else{ 
    alert('O numero A + B não é menor');
}
