//Percorrer uma Array

//Escreva um loop que percorra uma array e imprima cada elemento.

const minhaArray = [10, 20, 30, 40, 50];
for (let i = 0; i < minhaArray.length; i++) {
    console.log(minhaArray[i]);
}